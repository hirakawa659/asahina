# Hirakawa プロジェクト方針

## 設計原則
- **長文小説を前提にする**: 数万文字を超える長文でもパフォーマンスを維持する。
- **軽量動作の重視**: 描画負荷を最小限に抑え、軽快な入力を実現する。
- **大量DOM生成の禁止**: 1文字・1マスごとのReactコンポーネント化を避け、仮想化や効率的なDOM管理を行う。本文編集は通常のDOM/編集可能要素を基本とし、Canvasを本文編集領域の主体にはしない。
- **本文とグリッドの分離**: 本文のテキストデータと表示用のグリッドレイアウトを論理的に分離する。グリッド表示はCSS背景等で軽量に実装する。
- **論理座標系の一元化**: 文字、カーソル、グリッドを共通の論理座標（行・列）で管理する。

## 開発・パフォーマンス方針

* **完全な標準CSS化（Tailwind禁止）**: プロジェクト全体においてTailwind CSSの使用を一切禁止する。
* レイアウト、余白、文字サイズ、文字の太さ、装飾、エディタの動的伸縮（18〜40文字への追従）は、標準CSS、CSS変数、および計算式（`calc()`、`linear-gradient()` 等）で実装する。
* `p-4`、`text-lg`、`font-bold` などのTailwind CSSユーティリティクラスを使用しない。
* Tailwind CSSのユーティリティクラスを新規に追加したり、既存コードに導入したりしない。
* 既存のCSSクラス、CSS変数、コンポーネントのスタイルを優先して再利用する。
* UIを変更・追加する際は、まず `src/index.css` および関連する既存CSSを確認し、プロジェクト内の既存のデザインルールに合わせる。
* 新しいスタイルが必要な場合は、Tailwindではなく既存のCSS設計に沿って `src/index.css` または適切なCSSファイルに追加する。

## 実装方針
- **Local-first**: データはまずローカルに保存し、オフラインでも動作させる。
- **自動保存**: ユーザーの操作を失わないよう、常に最新の状態を保存する。
- **状態管理の一元化**: `appState` にアプリ全体の状態を集中させ、各モジュールで状態を分散させない。
- **責務の分離**: UIコンポーネントとエディタのロジック（入力・計算・管理）を明確に分離する。
- **必要な範囲の厳密な型防衛とフォールバック（対壊れデータ対策）**:
  - `appState` のスキーマを変更・拡張する際は、古いデータや破損データが原因でアプリが起動しなくなるバグ（デシリアライズエラー）を事前に防ぐコードを「常にセットで」実装すること。
  - Zod 等の外部ライブラリは一切使用せず、アプリの起動や正常な動作に必要な範囲（主要なキーやデータ型）に絞って、TypeScript 標準の型ガード（`typeof` チェック等）とデフォルト値のマージ処理を用いて自前で検証すること。
  - 巨大で過剰なバリデーション関数を生成することは避け、データ構造の主要部分に異常を検知した場合に、安全な初期値（デフォルト値）で自動補完し、不正データによる起動時クラッシュを可能な限り防ぎ、安全な初期値へフォールバックするよう軽量な構造を維持すること。
  - `AppState` 内のプロパティは原則としてオプショナル（`?`）にせず、初期値生成関数とデータ読み込み時の型ガードによって、常に安全な完全形の AppState を生成すること。

## 拡張性
- **将来のクラウド連携**: 将来的に Supabase 等のクラウド同期を追加できる構造にする。
- **表示モードの多様化**: 縦書き、白紙表示、ドット表示など、将来の表示モード追加を容易にする。

## 実行環境
- **環境差異の排除**: Cloudflare Pages と AI Studio Preview で同じコードベースを使用する。
- **実機検証**: iPad 等のタブレットデバイスでの動作を重要なテスト対象とする。
